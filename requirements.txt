# Core engine — no external dependencies needed
# All analysis runs on Python standard library (re, json, xml, sqlite3, csv, datetime)

# Optional — only needed for Signal message extraction:
pycryptodome~=3.19.0    # AES-GCM/CTR decryption for Signal backups
pysqlcipher3~=1.2.0     # SQLCipher access for Signal Desktop DB (requires C compiler + sqlcipher headers)

# Security — XML bomb / XXE protection:
defusedxml~=0.7.1       # Safe XML parsing (prevents billion-laughs, XXE, entity expansion)

# Async Task Queue
celery~=5.3.6
redis~=5.0.1

# Rate Limiting
slowapi~=0.1.9

# Demo
streamlit~=1.31.0

# Caching
cachetools~=5.3.2

# Monitoring
psutil~=5.9.8

# Load Testing
locust~=2.23.1
