# AI Configuration (optional — the tool works without these)
# Get your key from https://platform.openai.com/api-keys or https://console.anthropic.com/
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Model Selection
# Options: gpt-4-turbo, gpt-4o, claude-3-opus-20240229, claude-3-sonnet-20240229
AI_MODEL=gpt-4o

# Authentication (change these before running in any shared environment)
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme

# Encryption at Rest (optional — generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=
